# Archived 19-site scripts — DO NOT RUN

These are the pre-expansion (19-site) versions of the framework scripts. They write to the
SAME output paths as their `*_26.py` counterparts, so running them would silently overwrite
the current 26-site results in `papers/UNIFIED/results/O0_framework/`.

They are kept only for provenance. Use the `*_26.py` versions in the parent directory, or the
current pipeline in `scripts_pipeline/` (framework_state_dynamics.py, bayes_multilevel.py, etc.).
